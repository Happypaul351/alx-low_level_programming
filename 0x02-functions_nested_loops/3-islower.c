#include <stdio.h>
#include <ctype.h>

/**
 * _islower - function that check for lowercase character
 * @c: character in lowercase
 * Return: 1 or 0
 */

int _islower(int c)
{
	int c = 0;

	if (islower(c) == 0)
	{
		putchar

	return (0);

}
