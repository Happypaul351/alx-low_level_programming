#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>

/**
 * _islower - function that check for lowercase character
 * @c: character in lowercase
 * Return: 1 or 0
 */

int _islower(int c)
{
	int x = 'c';

	if (islower(x) == 0)
	{
		_putchar(x);

	return (1);
	}
	else
		return (0);


}
