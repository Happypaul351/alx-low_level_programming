#include <stdio.h>

/**
 * main - entrance point
 * Return: 0.
 */

int main(void)
{
	char alpbet = 'a';

	for (alpbet = 'a'; alpbet <= 'z'; alpbet++)
		putchar(alpbet);
	putchar('\n');

	return (0);
}
