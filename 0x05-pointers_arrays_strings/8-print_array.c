#include <stdio.h>
#include "main.h"

/**
 * print_array - function that prints n elements of an array of integer
 * @a: the array
 * @n: the element
 */

void print_array(int *a, int n)
{
	int *a;
	int n;
}
